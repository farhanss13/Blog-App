# No spaces around "=". Rebuild after changing (Vite reads env at build time).
# Local API:
VITE_BACKEND_URL=http://localhost:3000/api/v1
# Production (replace with your deployed backend base URL including /api/v1):
# VITE_BACKEND_URL=https://your-service.onrender.com/api/v1
